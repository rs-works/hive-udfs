version: 2.1
executors:
  java:
    docker:
      - image: openjdk:7-jdk-alpine
jobs:
  build:
    executor: java
    steps:
      - checkout
      - run: java -version
      - run: ./gradlew clean build
workflows:
  build:
    jobs:
      - build
